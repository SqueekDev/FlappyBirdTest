using UnityEngine;

public class ScoreZone : MonoBehaviour, IInteractable
{
    // Есть в видео как уже добавленный к объекту компонент, но ни про создание, ни про реализацию интерфейса ничего не говорится
    // про реализацию интерфейся можно догадаться по скрипту Bird, но не очевидно
}
